import logging
import os

LOG_LEVEL = os.getenv("LOG_LEVEL", "{{ default_log_level | default('INFO') }}")

def setup_logging():
    logging.basicConfig(
        level=LOG_LEVEL,
        format="%(asctime)s | %(levelname)s | %(name)s | %(message)s",
    )
